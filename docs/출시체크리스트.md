# 출시 체크리스트

Google Play 제출까지의 할 일. 순서대로 진행하는 게 낫다.

---

## 0. 실기기 테스트 (제일 먼저)

여기서 문제가 나오면 나머지는 다 무의미하다.

- [ ] 위쪽 절반이 180도 뒤집혀 마주 앉은 사람에게 바로 보이는지
- [ ] 탭 반응 지연이 없는지 (반응속도 게임의 생명)
- [ ] 소리가 나는지 (아이폰은 무음 스위치 확인)
- [ ] 삼총사 카드 12장이 안 잘리는지 (3열 × 4행)
- [ ] 같은 그림 찾기 원판 2개가 세로로 다 들어가는지
- [ ] 헷갈리는 아이콘 없는지 (곰↔강아지, 사과↔딸기)
- [ ] 제한 시간이 적당한지 (삼총사 6초 / 같은 그림 5초)

테스트: `web/index.html`을 폰으로 옮겨 브라우저로 열기 (갤럭시 A23 / 아이폰)

---

## 1. 글꼴 임베드 ⚠ 필수

지금은 Google Fonts CDN에 의존한다. **기내모드나 오프라인에서 글꼴이 깨진다.**

1. Jua, Gothic A1 내려받기 (둘 다 SIL Open Font License — 임베드·재배포 가능)
2. 한글은 글자 수가 많아 원본이 크다. 실제 쓰는 글자만 남기는 **서브셋** 처리 필요
   - `pyftsubset` (fonttools) 또는 `glyphhanger` 사용
   - 앱에 등장하는 한글만 추출해서 넣으면 수십 KB로 줄어든다
3. WOFF2로 변환 → base64 → `@font-face`로 HTML 안에 삽입
4. `<head>`의 CDN `<link>` 3줄 삭제
5. 기내모드로 켜서 글꼴이 유지되는지 확인

---

## 2. 이름 확정

- [ ] **키프리스(kipris.or.kr)에서 "쿵짝" 상표 검색** — 9류(소프트웨어), 41류(오락) 확인
- [ ] Play 스토어에서 동명 앱 검색
- [ ] 겹치면 대안 검토

⚠ 아이콘·스크린샷을 만든 뒤에 이름을 바꾸면 전부 다시 만들어야 한다. **이걸 먼저 끝낼 것.**

---

## 3. 아이콘 · 스토어 이미지

- [ ] 앱 아이콘 512×512 PNG (32비트, 알파 포함)
- [ ] 그래픽 이미지 1024×500 PNG
- [ ] 스크린샷 최소 2장 (권장 4~8장), 폰 세로 기준
- [ ] 캐릭터를 살린 아이콘 — 민트/코랄 캐릭터 둘이 마주 보는 구도가 컨셉과 맞음

---

## 4. 앱으로 감싸기

HTML 하나뿐이라 WebView 래핑이면 충분하다. 두 가지 선택지:

**A. Android 네이티브 WebView (권장)**
- 가볍고 빌드가 단순. Kotlin 프로젝트 하나에 WebView + assets
- 설정: JavaScript 허용, 확대 비활성, 캐시 사용, 오버스크롤 없음
- 화면 항상 켜짐(`FLAG_KEEP_SCREEN_ON`) — 대결 중 꺼지면 안 됨

**B. Flutter + webview_flutter**
- 이미 익숙한 스택. 나중에 iOS까지 갈 때 유리
- 대신 앱 용량이 커진다

- [ ] `applicationId` 확정 (`com.example` 기본값 금지)
- [ ] **target API 36** (2026년 8월 마감 기준)
- [ ] 릴리스 서명 키 생성 및 안전한 곳에 백업 — **분실하면 업데이트 불가**
- [ ] 세로 고정, 전체화면
- [ ] 뒤로가기 버튼 동작 (게임 중 → 메뉴, 메뉴 → 종료 확인)

---

## 5. Play Console 등록

- [ ] 스토어 등록정보 (제목, 짧은 설명 80자, 자세한 설명)
- [ ] 콘텐츠 등급 설문 (IARC) — 폭력·성적 요소 없음
- [ ] **타겟 대상 연령: 13세 이상** (아동 포함 시 Families 정책 적용됨)
- [ ] 데이터 안전 양식 — **수집 항목 없음**으로 신고 (광고 SDK 없으므로 간단)
- [ ] 개인정보처리방침 URL — 수집이 없어도 **URL은 필수**
- [ ] 광고 포함 여부: 아니오

### 개인 개발자 계정 주의

개인 계정은 **비공개 테스트 12명 이상 × 14일**을 거쳐야 프로덕션 출시가 열린다.
이 기간을 감안해 일정을 잡을 것. 테스터 모집이 병목이 되기 쉽다.

---

## 6. 출시 후 (선택)

지금은 광고도 결제도 없다. 반응을 보고 판단.

- 리텐션 장치: 누적 전적, 최고 기록, 게임 해금
- **AI 상대** — 혼자서도 열 수 있게 하는 가장 큰 개선. 없으면 재방문이 거의 없다
- 광고를 붙인다면 13세 이상 타겟이므로 제약은 없지만, 다운로드가 늘기 전엔 의미 없음

---

## 알려진 약점 (냉정하게)

- 게임 4개 vs 경쟁작 20~40개
- 상대가 옆에 있어야만 켤 수 있다 → 리텐션 구조적 약점
- 미니멀 디자인이라 스토어 썸네일에서 눈에 덜 띈다
- 마케팅 없이는 발견 자체가 어렵다

이 앱의 1차 목표는 **출시 파이프라인 학습**이다. 그 기준으로는 충분하다.
