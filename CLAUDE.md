# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 컨텍스트다.

---

## 프로젝트

**쿵짝** — 폰 한 대를 가운데 두고 두 사람이 마주 앉아 겨루는 미니게임 모음.
현재 게임 10개. Google Play 출시 준비 단계.

- 타겟 연령: **13세 이상** (아동 타겟이면 Families 광고 정책 제약이 커서 제외)
- 서버·계정·네트워크 통신 **없음**. 완전 오프라인
- 개인정보 수집 **없음**. localStorage도 아직 미사용

---

## 작업 원칙 (중요)

1. **한국어로 설명한다.** 명령 실행 전에는 무엇을 왜 하는지 먼저 알린다.
2. **한 번에 한 단계씩.** 여러 작업을 한꺼번에 실행하지 말고 단계마다 확인을 받는다.
3. **파일을 함부로 늘리지 않는다.** 새 파일이 필요하면 목적에 맞는 폴더에 넣는다.
4. **게임 본체는 단일 HTML 파일을 유지한다.** 빌드 도구, 번들러, npm 의존성을 추가하지 않는다.
5. **외부 CDN을 쓰지 않는다.** 오프라인에서 깨진다. 글꼴은 파일 안에 임베드되어 있고
   `index.html`에는 외부 참조가 하나도 없다. 이 상태를 유지한다.
6. 수정 후에는 인라인 스크립트를 추출해 `node --check`로 문법을 검증한다.
7. **화면에 나오는 한글 문구를 새로 추가했다면 `tools/embed_fonts.py`를 다시 돌린다.**
   글꼴은 실제로 쓰는 글자만 남긴 서브셋이라, 새 글자는 시스템 글꼴로 떨어져 톤이 깨진다.

---

## 파일 구조

⚠️ **로컬 폴더는 `C:\SYA\Two-player` 인데 저장소 이름은 `kungjjak` 이다** — 일부러 다르다.
폴더는 처음에 지은 이름 그대로 두고, 밖으로 나가는 이름만 앱 이름에 맞췄다.
다른 데서 `git clone` 하면 `kungjjak/` 로 받아진다.

| 무엇 | 이름 |
|---|---|
| 로컬 폴더 | `Two-player` |
| GitHub 저장소 | `SYA-Apps/kungjjak` (공개) — 기본 브랜치 `master` |
| 웹 주소 | **https://sya-apps.github.io/kungjjak/** (2026-09-02 배포) |
| 안드로이드 `applicationId` | `com.syaapps.kungjjak` — **콘솔에 앱을 만든 뒤라 영원히 못 바꾼다** |

📦 **새 공통 규칙은 `com.sya.<앱>` 인데 쿵짝은 예외로 남는다**(2026-09-16 사용자 «그대로 두기»).
바꾸려면 새 앱을 만들어 AAB·서명키·등록정보·답안·심사를 처음부터 다시 하고 검토 중인 제출을 버려야 한다.
**`applicationId` 를 고치지 말 것.** 콘솔 대시보드의 `com.syaapps.kungjjak (unreviewed)` 는 검토가 끝나면 사라지는 임시 이름이다.

로마자 표기는 `kungjjak` 이 맞다(ㅉ = `jj`). `kungjak` 은 "쿵작"이 된다.

```
kungjjak/                      # 저장소 기준. 이 PC 에서는 Two-player/
├── CLAUDE.md                  # 이 파일
├── README.md                  # 작업 이력과 다음 할 일
├── .gitignore
├── web/                       # 게임 본체 겸 PWA (그대로 웹 배포 가능)
│   ├── index.html             # 이 파일 하나가 게임 전부
│   ├── manifest.json
│   ├── sw.js                  # 오프라인 캐시. 수정 시 CACHE 버전 올릴 것
│   └── icons/                 # 앱 아이콘 (tools/make_icons.py 로 생성)
├── flutter/
│   ├── README.md              # 안드로이드 래핑 절차
│   └── main.dart              # WebView 껍데기. app/lib/main.dart 로 복사
├── tools/
│   ├── make_icons.py          # 아이콘 생성 스크립트
│   └── embed_fonts.py         # 글꼴 서브셋 + base64 임베드 (한글 문구 추가 시 재실행)
└── docs/
    ├── 출시체크리스트.md
    └── 아이폰-사파리-배포.md
```

**출시 전략**: 안드로이드는 Flutter WebView로 감싸 Play 스토어에 먼저 낸다.
아이폰은 `web/`을 GitHub Pages에 올려 사파리로 접속하게 한다 (네이티브는 나중).
`app/` 폴더(flutter create 결과물)는 커밋하지 않는다.

---

## 아키텍처

`web/index.html` 하나에 전부 들어 있다. 구조는 **셸 + 게임 모듈**.

| 구역 | 내용 |
|---|---|
| 1 | 캐릭터 — 표정 SVG 생성 (`MOODS`, `face()`) |
| 1-2 | 소리 — WebAudio로 생성. 음원 파일 없음 (`Sound`) |
| 1-3 | 배경음악 — 첫 화면에서만 흐르는 코드로 지은 곡 (`Music`) |
| 2 | 셸 — 화면 전환, 점수, 캐릭터 제어 (`ctx`) |
| 3-1~3-10 | 게임 10개 (`GAMES`) |
| 4 | 초기화 — 메뉴 카드 자동 생성 |

### 글꼴

`Jua`(제목)와 `Gothic A1`(본문 400·700)이 **base64 로 `index.html` 안에** 들어 있다.
`<style>` 맨 앞의 `FONTS-EMBED-START ~ END` 블록이 그것이고, **직접 고치지 않는다.**

```bash
python tools/embed_fonts.py    # 여러 번 돌려도 안전하다
```

이 스크립트가 원본을 받아 → 실제 쓰는 글자만 남기고 → woff2 로 줄여 → 다시 심는다.
둘 다 SIL Open Font License 라 임베드·재배포에 문제가 없다.
현재 517자(한글 397자) / woff2 합계 136KB / `index.html` 248KB.

### 게임 추가하는 법

`GAMES` 객체에 넣으면 메뉴 카드까지 자동 생성된다. 셸은 건드릴 필요 없다.

```js
GAMES.키 = {
  name: '이름', icon: '🎲', desc: '한 줄 설명',
  level: 2,                  // 난이도 1 쉬움 · 2 보통 · 3 어려움 (메뉴에 글자 알약으로 뜬다)
  layout: 'shared',          // 공용 판이면. 분할 화면이면 생략
  start(ctx){
    // 게임 로직
    return function stop(){ /* 타이머·리스너 정리 (필수) */ };
  }
};
```

### ctx API

| 함수 | 설명 |
|---|---|
| `setFace(p, mood)` | 표정 변경. `null`이면 캐릭터 숨김 |
| `setCaption(p, text)` | 캐릭터 아래 한 줄 |
| `setInfo(p, html)` | 보조 정보. HTML 허용 |
| `setPanel(p, html)` | 분할 레이아웃용 커스텀 UI 슬롯 |
| `setBoard(html, cls)` | 공용 판. `cls`는 `grid12` 또는 `duo` |
| `setSignal(on)` | 배경을 신호색으로 |
| `lock(p, on)` | 한쪽 버저 잠금 |
| `judge(p, ok)` | 정답/오답을 소리와 화면으로 동시 표시 |
| `award(winner)` | 득점. 승부가 끝나면 `true` 반환 |
| `tick()` | 초읽기용 짧은 소리 |
| `both(fn)` `other(p)` `half(p)` `board()` `score()` | 보조 |

`p`는 `'A'`(위쪽, 180도 회전) 또는 `'B'`(아래쪽).
표정: `calm` `tense` `alert` `win` `lose` `foul`

### 레이아웃 두 가지

- **분할** — 화면을 위아래로 나누고 위쪽을 180도 회전. 각자 자기 화면만 보면 되는 게임
  (번쩍 · 색깔 함정 · 오락가락 · 폭탄 돌리기 · 연타 대결)
- **공용 판** (`layout:'shared'`) — 위아래는 얇은 버저 띠가 되고 가운데 공용 판이 열린다. 같은 판을 함께 봐야 하는 게임
  (삼총사 · 같은 그림 찾기 · 딱 다섯 · 다른 색 하나 · 땅따먹기)

공용 판 게임의 그림·카드는 **180도 돌려도 같게 보이도록** 만들어야 한다.
(삼총사는 대칭 도형과 세로 줄무늬, 같은 그림 찾기는 그림마다 무작위 회전,
딱 다섯은 주사위 눈 배치라 뒤집어도 개수가 그대로 읽히고,
땅따먹기는 점 격자라 애초에 완전 대칭이다)

### 내기 점수

첫 화면에서 **5 · 10 · 15 · 20점** 중에 고른다. `winScore` 하나만 보면 되고
승리 판정(`ctx.award`)과 점수 표시(`drawPips`)가 함께 따라간다. 기본값은 10점.

⚠️ **2026-09-02 테스터 의견으로 넓혔다**(그전 1·2·3·5·7·10, 기본 3점). 한 판이 몇 초라
3점은 너무 빨리 끝났다. **다시 좁히지 말 것 — 임의로 정한 값이 아니다.**

점 표시는 20개까지 늘어난다. `.pips.many`(11점 이상)가 점을 11px→7px 로 줄인다.
안 줄이면 360px 폰에서 넘친다(20×11 + 19×7 = 353px).

**메모리에만 둔다 — `localStorage`를 쓰지 않는다.** 저장이 없어야
"기기에 아무것도 남기지 않는다"는 설명을 그대로 유지할 수 있다.
앱을 껐다 켜면 다시 10점으로 돌아온다.

### 랜덤 모드

첫 화면 맨 위 **랜덤** 카드. 한 점이 날 때마다 다른 게임으로 갈아탄다(점수는 이어진다).
갈아타기 전에 **다음 게임의 이름과 설명을 2초간 보여 준다** — 안 그러면 규칙을 모르는
판이 갑자기 시작돼 두 사람 다 뭘 해야 할지 모른다.

`randomMode` 가 켜져 있으면 `ctx.award` 가 **true 를 돌려준다.** 게임이 스스로 다음 판을
예약하지 못하게 막고 그 자리를 `nextRandom` 이 가져가기 위해서다.
안 그러면 옛 게임의 타이머와 새 게임이 겹쳐 돈다.

⚠️ **게임 안에서 `setTimeout` 을 쓸 때는 `stop()` 이 끊을 수 있게 모아 둘 것.**
랜덤 모드는 게임을 도중에 갈아치우므로, 안 끊으면 사라진 판을 다시 그린다.

### 오답 처리 — **틀려도 상대에게 점수를 주지 않는다**

2026-09-02 테스터 의견. 그전에는 6개 게임이 `ctx.award(ok ? p : o)` 로 오답 시 상대가
득점했다. **지금은 틀린 사람이 점수를 못 얻을 뿐이다.**

| 게임 | 틀렸을 때 |
|---|---|
| 삼총사 · 같은 그림 찾기 · 다른 색 하나 | **제한시간 안에서 계속 다시 고른다.** 시간이 다 되면 아무도 점수 없이 새 판 |
| 색깔 함정 · 딱 다섯 · 오락가락 | 한 번 누르면 끝나는 구조라 다시 고를 것이 없다. 아무도 점수 없이 새 판 |
| 번쩍(부정 출발) · 폭탄 돌리기 | **그대로 둔다.** 오답이 아니라 게임 규칙 자체라, 벌칙을 없애면 게임이 성립하지 않는다 |

---

## 디자인 토큰

| 항목 | 값 |
|---|---|
| 배경 | `#F2F1F8` |
| 글자 | `#2E2B3A` |
| 흐린 색 | `#DCDAE8` |
| 플레이어 A | `#7FD1B9` 민트 — 화면에서는 **`쿵`** |
| 플레이어 B | `#FF8FA3` 코랄 — 화면에서는 **`짝`** |
| 신호 | `#FFD166` |
| 정답 | `#3FB477` / 오답 `#E8544B` |

⚠️ **화면에 플레이어를 부를 때는 `쿵` / `짝` 을 쓴다.** 색 이름(민트·코랄)은 팔레트를
가리킬 때만 쓴다. **`빨강`은 절대 플레이어 이름으로 쓰지 말 것** — 빨강(`#E8544B`)이
이미 **오답 색**이라 "빨강이 승리" 와 "빨간 화면 = 틀림" 이 부딪힌다.
색과 무관한 이름이라 **색각 이상**(남성 약 8%)인 사람에게도 안전하다.

새 아이콘이나 UI를 만들 때는 이 팔레트 안에서 해결한다.
캐릭터·아이콘은 둥근 형태, 평면 색, 검정 점눈으로 통일.

---

## 검증 방법

```bash
# 인라인 스크립트 추출 후 문법 검사
node -e "
const fs=require('fs');
const s=fs.readFileSync('web/index.html','utf8');
fs.writeFileSync(process.env.TMP + '/chk.js', s.split('<script>')[1].split('</scr'+'ipt>')[0]);
" && node --check "$TMP/chk.js" && echo OK
```

**웹에 뭔가 새로 넣었으면 «밖에서 받아오는 게 생겼나» 를 본다.**
쿵짝은 «INTERNET 권한 없음 · 저장 없음» 이 스토어 답안(데이터 보안·등급·방침)의 뼈대라,
그 전제가 깨지는 순간 콘솔 답안과 스토어 설명이 한꺼번에 따라 움직인다(2026-09-20 ADMIN 확인).

```bash
grep -nE '<(script|link|img|audio|video|source|iframe)[^>]*(src|href)=|fetch\(|XMLHttpRequest|new Audio|@import|url\(https?:' web/index.html | grep -v 'data:'
```

나와도 되는 것은 **저장소 안 파일**(`./manifest.json` · `./icons/…`)뿐이다.
`og:image`·`og:url` 메타 두 줄은 카톡·검색이 긁어갈 때만 쓰는 값이라 실행 중에 부르지 않는다.
📌 소리도 **코드로 만든다**(`Sound`·`Music`) — 오디오 파일을 받아오지 않으니 이 전제가 저절로 지켜진다.

게임 규칙을 바꿨다면 수학적 검증도 다시 돌린다.
- 삼총사: 81장 덱의 정답 조합이 **1,080개**
- 같은 그림 찾기: 카드 31장, 모든 쌍(465가지)에서 겹치는 그림이 **정확히 1개**

---

## 하지 말 것

- 개인정보 수집, 네트워크 전송, 분석 SDK 추가
- 외부 CDN 스크립트나 폰트 추가
- SET / 도블 / 할리갈리 등 **상표명 사용**. 룰 차용은 문제없지만 이름과 아트는 직접 만든 것만 쓴다.
  **앱 안뿐 아니라 스토어 제목·설명문·스크린샷 문구에도 마찬가지다** — "○○ 같은 게임" 도 안 된다
- **아이폰에서 찍은 스크린샷을 스토어에 올리지 말 것.** 게임 카드에 이모지를 쓰는데
  애플 이모지 그림은 애플 저작권이다. 안드로이드(Noto)는 오픈 라이선스라 괜찮다
- 아동(만 13세 미만)을 타겟으로 하는 표현이나 아트 방향

### 🌐 온라인 기능을 넣는다면 — **콘솔 답안이 전부 뒤집힌다** (2026-09-16 ADMIN 전수 조사)

지금 쿵짝은 이용자 제작 콘텐츠(UGC) 안전장치가 **해당 없음**이다. 근거: 폰 한 대로 마주 앉아
서버에 «상대 사용자» 개념이 없고, 네트워크 호출 0건 · 입력칸(input/textarea/prompt) 0건 ·
저장소 0건 · **릴리스 빌드에 INTERNET 권한조차 없다**(디버그에만 있다) · 플레이어 이름은
코드에 박힌 «쿵»/«짝» 고정값. 그래서 IARC 에 «사용자 간 소통 아니요» 로 답한 것이 실물과 맞는다.

🚨 **온라인 대전 · 순위표 · 닉네임 입력 중 하나라도 붙이는 순간 위 답이 전부 거짓이 된다.**
넣기로 하면 **한 판에서 같이** 해야 한다 — 하나만 바꾸면 «실물과 답이 다르다» 로 반려·삭제된다:

1. **앱**: 신고 · 차단 · (호스트면) 내보내기. 본보기는 나란히의 `supabase/migrations/0006_report_block_kick.sql`
2. **콘솔**: IARC 설문 «사용자 간 소통 예» 로 다시 · 데이터 보안(무엇을 모으고 공유하는지) · 대상 연령 재검토
3. **개인정보처리방침**(`web/privacy.html`) · 스토어 설명의 «인터넷 권한 없이 동작합니다» 문구
4. **AndroidManifest** INTERNET 권한 · 서버(통합 Supabase) 설계 — 통합 서버 규칙은 `C:\SYA\CLAUDE.md` 「🗄️」

📌 **계획 단계에서 `_ADMIN-보고.md` 에 먼저 알린다.** 콘솔·서버는 ADMIN 몫이라 같이 움직여야 한다.

---

## 현재 상태와 다음 할 일

게임 10개 완성. PWA 설정·아이콘·글꼴 임베드 완료. 외부 의존 0건.
**이름 "쿵짝" 상표 확정 · 실기기(갤럭시 S8) 1차 테스트 완료 — 둘 다 2026-09-01.**
**웹 배포 완료 · 안드로이드 APK 빌드·설치·동작 확인 완료 — 둘 다 2026-09-02.**

**2026-09-01 게임 구성 변경**: **거짓말쟁이를 빼고 땅따먹기를 넣었다**(10개 유지).
거짓말쟁이는 ① 둘이서는 심리전이 성립하지 않아 사실상 찍기가 되고, ② "눈을 감으세요" 를
마주 앉은 상대가 지키는지 확인할 방법이 없으며, ③ 혼자만 말로 하는 턴제라 나머지와 결이
달랐다. 땅따먹기(점 잇기)는 **원래부터 2인 게임**이고 점 격자가 완전 대칭이라 공용 판에 맞는다.

✅ **이름은 확정됐다 — 다시 알아볼 필요 없다.** 키프리스에서 `쿵짝` 상표 12건을 확인했고,
살아 있는 9류 등록(하이하이 주식회사)은 **지정상품이 전부 휴대폰 액세서리**라 게임 앱과
안 부딪힌다. 41류(오락)·28류(게임기구)에는 `쿵짝` 이 아예 없다.
근거는 `docs/출시체크리스트.md` 2번에 표로 남겼다.

### 다음 순서 (2026-09-14 갱신) — **콘솔 등록 전까지는 거의 다 끝났다**

⏳ **막고 있는 것은 하나: 개발자 계정.** 조직(사업자) 계정을 만드는 중이고
D-U-N-S 번호를 기다린다(Case `#34828509`, 2026-09-08 제출, **9/17~9/26 예상**).
자세한 것은 `C:/SYA/CLAUDE.md` 의 「사업자(조직) 개발자 계정」 절.

📌 **9/12~13 에 한 것** — 같은 그림 찾기: 원판 찌그러짐·크기 흔들림 수정, 바나나(1.9배 굵기)·달(분화구)·
강아지(골든 리트리버) 다시 그림. 서비스워커를 «페이지는 네트워크 먼저» 로 바꿔 새로고침 한 번에 새 게임이 뜬다.
S8 에서 기계로 잰 결과는 `docs/출시체크리스트.md` 0번. 웹 캐시 **v24** · S8 앱 설치 · 커밋 `ed2754e` 까지 전부 맞춰져 있다.
S8 은 무선 adb(`192.168.219.116:5555`)로도 붙어 있다 — 폰을 껐다 켜면 풀린다.

### 🌐 웹에만 있는 것 — **다음 AAB 에 들어갈 것** (공용 규칙 «웹에서 먼저 → 모아서 한 판으로»)

지금 프로덕션 심사 중이라 **AAB 를 새로 올리지 않는다**(올리면 검토가 처음부터 다시 돈다).
웹에서 먼저 내고 의견을 받아 고친 뒤, 심사가 끝나면 **최종본 하나로** 앱에 넣는다.

✅ **2026-09-20 사용자 «맘에 들어» — 아래 셋은 이대로 확정.** 더 고칠 것이 없으므로
심사가 끝나면 **`rm -rf app/assets/web && cp -r web app/assets/web` → AAB 빌드 → ADMIN 에 업로드 요청**
한 판이면 된다(웹 캐시 `v30` 기준). 그때 `pubspec.yaml` 의 `+N` 을 올리는 것을 잊지 말 것.

| 언제 | 무엇 | 웹 |
|---|---|---|
| 2026-09-20 | **첫 화면 배경음악**(`Music`) — 음원 파일 없이 코드로 지은 폴카. 오른쪽 위 단추로 끄고 켠다.
  의견 세 번 반영: 음량 두 배(v26) · 경쾌하게 132BPM + 엇박 «찰» + 8분음표 가락(v27) · **리듬만 조금 천천히 120BPM**(v29) | 캐시 `v29` |
| 2026-09-20 | **로고가 굴러와서 쿵 박는다** — 「쿵」 왼쪽 · 「짝」 오른쪽에서 한 바퀴 반씩 구르며 1.6초에 걸쳐 와서
  가운데서 멈춘다(70% 지점). 그 순간 «쿵!» 소리(`Music.bump`). 첫 화면에 올 때마다 다시 돈다.
  박는 순간 첫 화면 전체가 0.36초 짧게 흔들린다(v30).
  📌 처음엔 0.9초에 «날아오는» 모양이었는데 «너무 빠르다 · 천천히 굴러오는 게 좋다» 는 의견으로 바꿨다 | 캐시 `v30` |


| | 할 일 | 상태 |
|---|---|---|
| 1 | **릴리스 서명 키 만들기** (`flutter/README.md` 7번) | ✅ **2026-09-11** — 백업 두 곳 · AAB·APK 서명 확인 |
| 2 | 스토어 자산 | ✅ 아이콘·피처그래픽(2배본 포함) 완료 |
| 3 | 콘솔 설문 답안 | ✅ `docs/콘솔-설문-답안.md` 에 근거까지 |
| 4 | 스크린샷 | ✅ **2026-09-15** — S8 설치 앱에서 8장 새로 찍음 |
| 5 | 개발자 계정 · 콘솔 등록 | ✅ 조직 계정 `Sodam Apps` · 앱 생성(2026-09-15, ADMIN) · 내부 테스트 `1 (1.0.0)` · ⏳ **프로덕션 검토 제출 2026-09-16(대한민국만) — 보통 7일, 승인되면 바로 공개**. 🔁 **콘솔 입력·AAB 업로드는 ADMIN 이 한다**(사용자 지시) — 이 세션은 콘솔을 열지 않고 준비물만 `_ADMIN-보고.md` 에 넘긴다 |
| 6 | 사람이 폰을 쥐고 하는 확인 | ✅ **2026-09-15 끝** — 기계로 잰 것(9/13 S8 · 9/15 앱으로 게임 10개) + 사용자 확인(소리 들림 · 제한 시간·다른 색 하나·딱 다섯 1.7초는 **지금 값 그대로**) |

🔑 **서명 키는 `C:/SYA/keys/kungjjak-upload.jks` 에 있다**(2026-09-11 생성).
키를 잃으면 앱을 영영 업데이트할 수 없다. 백업은 공용 도구가 **두 곳에** 넣었다:

```
python C:/SYA/tools/backup_signing_key.py --app 쿵짝    # 상태만 본다
```

- `OneDrive/SYA-앱-서명키/쿵짝/` · `D:/SYA-백업/업로드키/쿵짝/` — 원본과 해시 일치 확인
- 지문(SHA-256) `C8:DA:CA:5B:…:DE:22:55:CF` — **첫 업로드 뒤 콘솔의 «업로드 키 인증서» 와 같아야 한다**
- 비밀번호는 `app/android/key.properties` 에만 있다(커밋 안 됨). 사람이 따로 적어 둔 곳은
  공용 문서가 말하는 구글 드라이브 「개발앱 업로드 키」 문서다 — ✅ **쿵짝 것도 적어 뒀다(2026-09-15 사용자 확인)**
- 📌 **키를 `app/` 안에 두지 않는 이유** — `flutter create` 로 다시 만드는 폴더라 같이 날아간다.
  홈 폴더도 안 쓴다 — 경로에 윈도우 사용자명이 들어가 이 공개 저장소 문서에 드러난다.

## 웹에 반영하는 법 — **push 만으로는 안 바뀐다**

사이트는 `master` 가 아니라 **`gh-pages` 브랜치의 루트**를 본다
(Pages 는 소스 폴더로 루트나 `/docs` 만 고를 수 있어 `/web` 을 못 쓴다).
그래서 게임을 고치면 **두 번** 올려야 한다.

```bash
git push                                        # 소스
git subtree push --prefix web origin gh-pages   # 배포본 ← 이걸 잊으면 사이트 그대로
```

`web/index.html` 을 고쳤으면 `web/sw.js` 의 `CACHE` 버전도 올릴 것.
**안 올리면 이미 방문한 사람에게는 옛 파일이 계속 나간다**(2026-09-01 에 실제로
실기기가 백지로 보여 한참 헤맸는데, 원인이 남아 있던 옛 서비스워커였다).

📌 **2026-09-13 부터 페이지(`index.html`)는 네트워크 먼저다** — 인터넷이 되면 새로고침
한 번에 새 게임이 뜨고, 안 되면 캐시로 뜬다. 그전에는 전부 캐시 먼저라 배포해도
아이폰에 옛 게임이 계속 떴다(«두 번 새로고침» 해도 안 바뀐 적이 있다).
아이콘·manifest 는 여전히 캐시 먼저이므로 **`CACHE` 버전은 계속 올린다.**
⚠️ 브라우저가 페이지를 최대 10분 들고 있을 수 있다(GitHub Pages `max-age=600`).
배포 직후 안 바뀌어 보이면 몇 분 뒤 다시 본다. 확인은 개인정보 보호(시크릿) 탭이 가장 확실하다.

## 앱에 반영하는 법 — **여기도 복사를 잊으면 옛 게임이 나간다**

안드로이드 앱은 `web/` 을 **복사해 넣은** `app/assets/web/` 을 읽는다. 웹과 달리
자동으로 따라가지 않는다. 게임을 고쳤으면:

```bash
rm -rf app/assets/web && cp -r web app/assets/web    # ← 이걸 잊으면 옛 게임이 빌드된다
cd app && flutter build apk --release
adb install -r build/app/outputs/flutter-apk/app-release.apk
```

🚨 **`INSTALL_FAILED_UPDATE_INCOMPATIBLE` 이 나오면 서명이 바뀐 것이다** (2026-09-11 겪었다).
서명 키를 만든 뒤로 빌드는 **업로드 키**로 서명되는데, 그 전에 깐 것은 **디버그 키**다.
안드로이드는 서명이 다른 앱을 덮어 깔지 않는다. **한 번 지우고 새로 깐다:**

```bash
adb -s <기기> uninstall com.syaapps.kungjjak    # 쿵짝은 기기에 아무것도 저장 안 해 잃을 게 없다
adb -s <기기> install build/app/outputs/flutter-apk/app-release.apk
```

이건 **기기마다 딱 한 번**이다. 한 번 업로드 키판을 깔고 나면 다음부터는 `install -r` 이 그대로 된다.
⚠️ 스토어에 올린 뒤 **테스터 폰에 옛 디버그판이 깔려 있으면 같은 이유로 스토어 업데이트가 안 된다.**
테스터에게 «지우고 스토어에서 받으라» 고 알려야 한다.

`app/` 은 `.gitignore` 에 들어 있어 커밋되지 않는다(`flutter create` 결과물이라
언제든 다시 만들 수 있다). 커밋되는 것은 `flutter/main.dart` 와 `flutter/README.md` 뿐이니,
**앱 껍데기를 고칠 때는 `flutter/main.dart` 를 고치고 `app/lib/main.dart` 로 복사한다.**
반대로 하면 다음에 `app/` 을 다시 만들 때 날아간다.

---

**게임 로직을 Dart로 옮기지 말 것.** 웹과 앱이 같은 `web/index.html`을 공유해야 한다.
